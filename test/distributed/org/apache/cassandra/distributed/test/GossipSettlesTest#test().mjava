    @Test
    public void test()
    {
        try (Cluster cluster = Cluster.create(3, config -> config.with(GOSSIP).with(NETWORK)))
        {
        }
        catch (IOException e)
        {
            e.printStackTrace();
        }
    }

