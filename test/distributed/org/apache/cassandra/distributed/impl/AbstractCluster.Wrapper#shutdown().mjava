        @Override
        public synchronized void shutdown()
        {
            if (isShutdown)
                throw new IllegalStateException();
            isShutdown = true;
            delegate.shutdown();
            delegate = null;
        }

