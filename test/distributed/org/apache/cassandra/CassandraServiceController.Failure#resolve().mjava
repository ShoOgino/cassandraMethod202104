        public void resolve()
        {
            callOnHosts(hosts, "start_cassandra");
            for (InetAddress host : hosts)
            {
                waitForNodeInitialization(host);
            }
        }

