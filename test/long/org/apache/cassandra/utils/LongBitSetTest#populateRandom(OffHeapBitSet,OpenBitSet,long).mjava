    public void populateRandom(OffHeapBitSet offbs, OpenBitSet obs, long index)
    {
        if (random.nextBoolean())
        {
            offbs.set(index);
            obs.set(index);
        }
    }

