    @Test
    public void roundTripSafetyProperty()
    {
        qt().withExamples(35)
            .forAll(inputs(),
                    compressors(),
                    checksumTypes(),
                    blocksizes())
            .checkAssert(this::roundTrip);
    }

