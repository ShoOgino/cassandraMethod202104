    @Test
    public void roundTripZeroLengthInput()
    {
        qt().withExamples(20)
            .forAll(zeroLengthInputs(),
                    compressors(),
                    checksumTypes(),
                    blocksizes())
            .checkAssert(this::roundTrip);
    }

