    @Test
    public void testSizeWhenNotEmpty()
    {
        queue.offer(0);
        assertEquals(1, queue.size());

        for (int i = 1; i < 100; i++)
            queue.offer(i);
        assertEquals(100, queue.size());
    }

