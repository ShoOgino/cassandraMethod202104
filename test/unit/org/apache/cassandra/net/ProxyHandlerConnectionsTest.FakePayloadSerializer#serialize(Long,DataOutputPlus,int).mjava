        public void serialize(Long i, DataOutputPlus out, int version) throws IOException
        {
            for (int j = 0; j < size; j++)
            {
                out.writeLong(i);
            }
        }

