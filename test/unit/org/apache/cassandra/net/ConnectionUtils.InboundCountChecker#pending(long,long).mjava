        public InboundCountChecker pending(long count, long bytes)
        {
            scheduled = count;
            scheduledBytes = bytes;
            checkScheduled = true;
            return this;
        }

