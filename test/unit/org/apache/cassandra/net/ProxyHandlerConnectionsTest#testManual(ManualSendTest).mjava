    private void testManual(ManualSendTest test) throws Throwable
    {
        for (ConnectionTest.Settings s: SETTINGS)
        {
            doTestManual(s, test);
            cleanup();
        }
    }

