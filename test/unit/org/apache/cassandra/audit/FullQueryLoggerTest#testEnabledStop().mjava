    @Test
    public void testEnabledStop() throws Exception
    {
        assertFalse(instance.enabled());
        configureFQL();
        assertTrue(instance.enabled());
        instance.stop();
        assertFalse(instance.enabled());
    }

