        public synchronized void fail(Executor executor)
        {
            failCalled = true;
            if (onFail != null)
            {
                onFail.run();
            }
            super.fail(executor);
        }

