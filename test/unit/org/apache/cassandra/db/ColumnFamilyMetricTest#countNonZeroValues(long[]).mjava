    private static int countNonZeroValues(long[] array)
    {
        int count = 0;
        for (long value : array)
        {
            if (value != 0)
                count++;
        }
        return count;
    }

