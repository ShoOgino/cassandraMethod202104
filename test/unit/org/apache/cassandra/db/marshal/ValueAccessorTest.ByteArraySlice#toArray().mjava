        /**
         * Returns the silce as a byte array.
         */
        public byte[] toArray()
        {
            return Arrays.copyOfRange(originalArray, offset, offset + length);
        }

