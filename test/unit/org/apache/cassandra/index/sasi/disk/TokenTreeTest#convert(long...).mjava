    private static LongSet convert(long... values)
    {
        LongSet result = new LongHashSet(values.length);
        for (long v : values)
            result.add(v);

        return result;
    }

