    private void onComplete()
    {
        if (stdin != null)
        {
            try
            {
                stdin.close();
            }
            catch (IOException e)
            {
                logger.warn("Error closing stdin for {}", allArgs, e);
            }
        }
    }

