    private static int firstdiff(byte[] canon, byte[] test)
    {
        for (int i = 0 ; i < canon.length ; i++)
            if (canon[i] != test[i])
                return i;
        return -1;
    }

