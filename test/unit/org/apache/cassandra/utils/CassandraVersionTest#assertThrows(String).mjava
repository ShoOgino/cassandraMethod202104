    private static void assertThrows(String str)
    {
        try
        {
            new CassandraVersion(str);
            assert false;
        }
        catch (IllegalArgumentException e) {}
    }

