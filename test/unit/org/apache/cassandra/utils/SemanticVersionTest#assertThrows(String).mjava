    private static void assertThrows(String str)
    {
        try
        {
            new SemanticVersion(str);
            assert false;
        }
        catch (IllegalArgumentException e) {}
    }

