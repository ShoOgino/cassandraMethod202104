    @Before
    public void setUp() throws Exception
    {
        path = tempDir();
        binLog = new BinLog(path, RollCycles.TEST_SECONDLY, 10, 1024 * 1024 * 128);
        binLog.start();
    }

