    public static String repeat(String v, Integer n)
    {
        StringBuilder sb = new StringBuilder();
        for (int i = 0; i < n; i++)
            sb.append(v);
        return sb.toString();
    }

