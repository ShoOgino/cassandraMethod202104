    protected ByteBuffer executeImpl(ProtocolVersion protocolVersion, List<ByteBuffer> params)
    {
        synchronized (this)
        {
            Math.sin(1d);
        }
        return null;
    }

