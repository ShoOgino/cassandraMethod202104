    public void truncate(long toSize)
    {
        try
        {
            channel.truncate(toSize);
        }
        catch (IOException e)
        {
            throw new FSWriteError(e, getPath());
        }
    }

