    public void recycle()
    {
        assert handle != null;

        if (buffer().capacity() <= MAX_RECYCLE_BUFFER_SIZE)
        {
            buffer.rewind();
            RECYCLER.recycle(this, handle);
        }
    }

