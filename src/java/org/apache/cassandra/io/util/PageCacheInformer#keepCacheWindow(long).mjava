    /**
     * Instruct the OS to keep a portion of the page cache from startAt to current position.
     * this call is only appropriate for writes
     * @param startAt the start of the window
     */
    void keepCacheWindow(long startAt);

