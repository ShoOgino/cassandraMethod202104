    @Override
    public void close()
    {
        Throwables.perform(channel.filePath(), Throwables.FileOpType.READ,
                           super::close,
                           validator::close,
                           channel::close);
    }

