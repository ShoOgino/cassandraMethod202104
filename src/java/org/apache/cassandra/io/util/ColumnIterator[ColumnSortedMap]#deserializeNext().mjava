    private IColumn deserializeNext()
    {
        try
        {
            count++;
            return serializer.deserialize(dis, expireBefore);
        }
        catch (IOException e)
        {
            throw new IOError(e);
        }
    }

