    private IColumn deserializeNext()
    {
        try
        {
            count++;
            return serializer.deserialize(dis, interner, fromRemote);
        }
        catch (IOException e)
        {
            throw new IOError(e);
        }
    }

