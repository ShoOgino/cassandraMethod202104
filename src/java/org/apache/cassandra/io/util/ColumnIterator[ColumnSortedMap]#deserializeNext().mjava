    private IColumn deserializeNext()
    {
        try
        {
            count++;
            return serializer.deserialize(dis, interner);
        }
        catch (IOException e)
        {
            throw new IOError(e);
        }
    }

