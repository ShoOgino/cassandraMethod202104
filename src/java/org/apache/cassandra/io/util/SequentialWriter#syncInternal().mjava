    protected void syncInternal() throws IOException
    {
        if (syncNeeded)
        {
            flushInternal();
            out.getFD().sync();

            syncNeeded = false;
        }
    }

