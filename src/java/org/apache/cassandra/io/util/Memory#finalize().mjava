    @Override
    protected void finalize() throws Throwable
    {
        try
        {
            if (peer != 0)
                free();
        }
        finally
        {
            super.finalize();
        }
    }

