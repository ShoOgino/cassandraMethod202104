        public void writeChunkSize(int length)
        {
            try
            {
                writer.stream.writeInt(length);
            }
            catch (IOException e)
            {
                throw new IOError(e);
            }
        }

