        public int size()
        {
            int size = super.size();
            /* add the size of the timestamp which is a long */ 
            size += 8;
            return size;
        }

