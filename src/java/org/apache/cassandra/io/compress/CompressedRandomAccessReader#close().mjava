    @Override
    public void close()
    {
        super.close();
        try
        {
            source.close();
        }
        catch (IOException e)
        {
            throw new FSReadError(e, getPath());
        }
    }

