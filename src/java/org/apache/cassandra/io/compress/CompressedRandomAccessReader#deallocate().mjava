    @Override
    public void deallocate()
    {
        super.deallocate();

        try
        {
            source.close();
        }
        catch (IOException e)
        {
            throw new FSReadError(e, getPath());
        }
    }

