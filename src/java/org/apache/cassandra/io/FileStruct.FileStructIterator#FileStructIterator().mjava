        public FileStructIterator()
        {
            if (row == null)
            {
                if (!isExhausted())
                {
                    forward();
                }
            }
        }

