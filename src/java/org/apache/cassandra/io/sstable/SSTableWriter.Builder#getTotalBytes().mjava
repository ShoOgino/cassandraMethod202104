        public long getTotalBytes()
        {
            maybeOpenIndexer();
            try
            {
                // (length is still valid post-close)
                return indexer.dfile.length();
            }
            catch (IOException e)
            {
                throw new IOError(e);
            }
        }

