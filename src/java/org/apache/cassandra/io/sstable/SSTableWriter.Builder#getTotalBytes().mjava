        public long getTotalBytes()
        {
            maybeOpenFile();
            try
            {
                // (length is still valid post-close)
                return dfile.length();
            }
            catch (IOException e)
            {
                throw new IOError(e);
            }
        }

