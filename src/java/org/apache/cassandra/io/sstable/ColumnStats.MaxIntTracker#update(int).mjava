        public void update(int value)
        {
            if (!isSet)
            {
                this.value = value;
                isSet = true;
            }
            else
            {
                if (value > this.value)
                    this.value = value;
            }
        }

