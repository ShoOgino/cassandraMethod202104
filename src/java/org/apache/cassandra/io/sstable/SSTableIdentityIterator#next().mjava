    public IColumn next()
    {
        try
        {
            return sstable.getColumnSerializer().deserialize(file, expireBefore);
        }
        catch (IOException e)
        {
            throw new IOError(e);
        }
    }

