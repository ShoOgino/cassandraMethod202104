        protected String computeNext()
        {
            if (isExhausted())
            {
                return endOfData();
            }
            String oldKey = key;
            forward();
            return oldKey;
        }

