        protected String computeNext()
        {
            if (isExhausted())
            {
                return endOfData();
            }
            String oldKey = getKey();
            forward();
            return oldKey;
        }

