    private boolean startSessionIfRequested()
    {
        if (state().traceNextQuery())
        {
            state().createSession();
            return true;
        }
        return false;
    }

