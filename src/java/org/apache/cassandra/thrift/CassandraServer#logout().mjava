    public void logout()
    {
        keySpace.remove();
        loginDone.remove();
        StorageProxy.disableSessionConsistency();

        if (logger.isDebugEnabled())
            logger.debug("logout complete");
    }

