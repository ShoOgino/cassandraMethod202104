        public DCPermissions build()
        {
            if (dcs.isEmpty())
            {
                return DCPermissions.all();
            }
            else
            {
                return subset(dcs);
            }
        }

