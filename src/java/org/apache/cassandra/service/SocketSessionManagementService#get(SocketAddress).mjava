    public ClientState get(SocketAddress key)
    {
        ClientState retval = null;
        if (key != null)
        {
            retval = activeSocketSessions.get(key);
        }
        return retval;
    }

