            @Override
            public Row staticRow()
            {
                Row staticRow = super.staticRow();
                if (!staticRow.isEmpty())
                    lastRow = staticRow;
                return staticRow;
            }

