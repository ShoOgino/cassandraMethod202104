        /**
         * clear all RepairJobs and terminate this session.
         */
        public void forceShutdown()
        {
            jobs.clear();
            activeJobs.clear();
            differencingDone.signalAll();
            completed.signalAll();
        }

