    public void reset()
    {
        user = DatabaseDescriptor.getAuthenticator().defaultUser();
        keyspace = null;
        preparedTracingSession = null;
        resourceClear();
        prepared.clear();
    }

