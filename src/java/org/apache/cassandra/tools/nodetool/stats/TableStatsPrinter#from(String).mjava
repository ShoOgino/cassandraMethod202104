    public static TableStatsPrinter from(String format)
    {
        switch (format)
        {
            case "json":
                return JSON;
            case "yaml":
                return YAML;
            default:
                return DEFAULT;
        }
    }

