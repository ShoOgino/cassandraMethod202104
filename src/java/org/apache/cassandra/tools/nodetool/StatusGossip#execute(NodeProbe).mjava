    @Override
    public void execute(NodeProbe probe)
    {
        probe.output().out.println(
                probe.isGossipRunning()
                ? "running"
                : "not running");
    }

