    protected void turnOffInterestOps(SelectionKey key, int ops)
    {
        synchronized(gate)
        {
            selector.wakeup();
            key.interestOps(key.interestOps() & (~ops) );
        }
    }

