    Header(String id, InetAddress from, Stage messageType, StorageService.Verb verb)
    {
        assert id != null;
        assert from != null;
        assert messageType != null;
        assert verb != null;

        messageId_ = id;
        from_ = from;
        type_ = messageType;
        verb_ = verb;        
    }

