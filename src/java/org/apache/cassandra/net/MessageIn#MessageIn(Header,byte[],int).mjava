    public MessageIn(Header header, byte[] body, int version)
    {
        assert header != null;
        assert body != null;

        this.header = header;
        this.body = body;
        this.version = version;
    }

