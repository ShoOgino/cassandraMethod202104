    public IncomingTcpConnection(Socket socket)
    {
        assert socket != null;
        this.socket = socket;
        try
        {
            input = new DataInputStream(socket.getInputStream());
        }
        catch (IOException e)
        {
            close();
            throw new IOError(e);
        }
    }

