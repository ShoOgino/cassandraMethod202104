    void removeConnection(TcpConnection connection)
    {
        lock_.lock();
        try
        {
            allConnections_.remove(connection);
        }
        finally
        {
            lock_.unlock();
        }
    }

