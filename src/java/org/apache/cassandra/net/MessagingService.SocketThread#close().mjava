        void close() throws IOException
        {
            logger.debug("Closing accept() thread");
            server.close();
            for (Closeable connection : connections) 
            {
                connection.close();
            }
        }

