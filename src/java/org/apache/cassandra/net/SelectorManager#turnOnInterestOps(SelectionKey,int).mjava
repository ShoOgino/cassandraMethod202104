    protected void turnOnInterestOps(SelectionKey key, int ops)
    {
        synchronized(gate)
        {
            selector.wakeup();
            key.interestOps(key.interestOps() | ops);
        }
    }

