    Header(String id, InetAddress from, StorageService.Verb verb)
    {
        assert id != null;
        assert from != null;
        assert verb != null;

        messageId_ = id;
        from_ = from;
        verb_ = verb;
    }

