    synchronized void destroy(TcpConnection con)
    {
        assert con != null;
        if (cmdCon == con)
        {
            cmdCon = null;
        }
        else
        {
            assert ackCon == con;
            ackCon = null;
        }
    }

