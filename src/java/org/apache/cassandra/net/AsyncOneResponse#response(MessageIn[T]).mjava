    public synchronized void response(MessageIn<T> response)
    {
        if (!done)
        {
            result = response.payload;
            done = true;
            this.notifyAll();
        }
    }

