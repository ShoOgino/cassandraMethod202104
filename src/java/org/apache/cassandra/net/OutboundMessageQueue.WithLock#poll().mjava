        Message<?> poll()
        {
            Message<?> m;
            while (null != (m = internalQueue.poll()))
            {
                if (shouldSend(m, nowNanos))
                    break;

                onExpired.accept(m);
            }

            return m;
        }

