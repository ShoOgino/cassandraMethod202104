    boolean contains(TcpConnection connection)
    {
        lock_.lock();
        try
        {
            return allConnections_.contains(connection);
        }
        finally
        {
            lock_.unlock();
        }
    }

