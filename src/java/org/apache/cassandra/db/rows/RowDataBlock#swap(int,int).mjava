    // Swap row i and j
    public void swap(int i, int j)
    {
        if (simpleData != null)
            simpleData.swap(i, j);
        if (complexData != null)
            complexData.swap(i, j);
    }

