        @Override
        public void addCell(Cell cell)
        {
            setColumn(cell.column());
            super.addCell(cell);
            if (column.isComplex())
                complexColumnCellsCount[complexColumnIndex] += 1;
        }

