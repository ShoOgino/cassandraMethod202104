    // Move row i into j
    public void move(int i, int j)
    {
        int s = columns.simpleColumnCount();
        for (int k = 0; k < s; k++)
            data.moveCell(i * s + k, j * s + k);
    }

