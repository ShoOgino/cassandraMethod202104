    public boolean isEmpty()
    {
        return !primaryKeyLivenessInfo().hasTimestamp()
            && deletion().isLive()
            && !iterator().hasNext()
            && !hasComplexDeletion();
    }

