            public void reduce(int idx, ColumnData data)
            {
                if (useColumnMetadata(data.column()))
                    column = data.column();

                versions.add(data);
            }

