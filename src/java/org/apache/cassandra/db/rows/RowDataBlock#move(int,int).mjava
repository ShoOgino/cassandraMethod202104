    // Move row i into j
    public void move(int i, int j)
    {
        if (simpleData != null)
            simpleData.move(i, j);
        if (complexData != null)
            complexData.move(i, j);
    }

