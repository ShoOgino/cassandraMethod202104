        private void findNextColumnWithCells()
        {
            while (columnIdx < endColumnIdx)
            {
                resetCellIdx();
                if (idx < endIdx)
                    return;
                ++columnIdx;
            }
        }

