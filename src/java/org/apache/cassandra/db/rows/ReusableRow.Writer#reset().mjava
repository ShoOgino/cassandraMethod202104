        @Override
        public Writer reset()
        {
            super.reset();
            clustering.reset();
            liveness.reset();
            deletion = DeletionTime.LIVE;
            return this;
        }

