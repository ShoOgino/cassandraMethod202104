        // Move row i into j
        protected void moveCells(int i, int j)
        {
            int max = Math.max(i, j);
            if (max >= cells.size())
                ensureCapacity(max);

            cells.set(j, cells.get(i));
        }

