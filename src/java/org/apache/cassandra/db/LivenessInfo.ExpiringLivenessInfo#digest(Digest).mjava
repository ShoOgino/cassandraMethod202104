        @Override
        public void digest(Digest digest)
        {
            super.digest(digest);
            digest.updateWithInt(localExpirationTime)
                  .updateWithInt(ttl);
        }

