    SuperColumn(ByteBuffer name, ISortedColumns columns)
    {
        super(columns);
        assert name != null;
        assert name.remaining() <= IColumn.MAX_NAME_LENGTH;
        this.name = name;
    }

