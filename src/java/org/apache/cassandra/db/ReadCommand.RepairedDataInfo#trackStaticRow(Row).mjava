        void trackStaticRow(Row row)
        {
            assert purger != null;
            Row purged = purger.applyToRow(row);
            if (!purged.isEmpty())
            {
                isFullyPurged = false;
                purged.digest(getPerPartitionDigest());
            }
        }

