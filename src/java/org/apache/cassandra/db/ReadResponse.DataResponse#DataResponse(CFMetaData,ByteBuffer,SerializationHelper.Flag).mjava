        protected DataResponse(CFMetaData metadata, ByteBuffer data, SerializationHelper.Flag flag)
        {
            super(metadata);
            this.data = data;
            this.flag = flag;
        }

