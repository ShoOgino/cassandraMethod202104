        AntiCompactor(ColumnFamilyStore cfstore, Collection<Range> ranges, InetAddress target)
        {
            super(cfstore);
            this.ranges = ranges;
            this.target = target;
        }

