    public static synchronized DBManager instance() throws IOException
    {
        if (dbMgr_ == null)
        {
           dbMgr_ = new DBManager();
        }
        return dbMgr_;
    }

