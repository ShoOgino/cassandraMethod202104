    @Override
    public String toString()
    {
        if (isFetchAll && queried == null)
            return "*/*";

        String prefix = "";
        if (isFetchAll)
            prefix = "*/";

        if (queried.isEmpty())
            return prefix + "[]";

        return prefix + toString(false);
    }

