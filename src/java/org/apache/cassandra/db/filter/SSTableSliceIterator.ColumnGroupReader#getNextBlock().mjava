        public boolean getNextBlock() throws IOException
        {
            boolean result = getBlockFromCurIndex();
            if (isAscending_)
                curRangeIndex_++;
            else
                curRangeIndex_--;
            return result;
        }

