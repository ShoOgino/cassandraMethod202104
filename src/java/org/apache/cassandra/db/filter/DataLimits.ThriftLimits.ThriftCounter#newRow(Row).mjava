            public void newRow(Row row)
            {
                for (Cell cell : row)
                {
                    if (assumeLiveData || cell.isLive(nowInSec))
                    {
                        ++cellsCounted;
                        ++cellsInCurrentPartition;
                    }
                }
            }

