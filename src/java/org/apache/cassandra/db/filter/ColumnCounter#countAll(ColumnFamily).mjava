    public ColumnCounter countAll(ColumnFamily container)
    {
        if (container == null)
            return this;

        for (Column c : container)
            count(c, container);
        return this;
    }

