            public void newRow(Row row)
            {
                if (assumeLiveData || row.hasLiveData(nowInSec))
                {
                    ++rowCounted;
                    ++rowInCurrentPartition;
                }
            }

