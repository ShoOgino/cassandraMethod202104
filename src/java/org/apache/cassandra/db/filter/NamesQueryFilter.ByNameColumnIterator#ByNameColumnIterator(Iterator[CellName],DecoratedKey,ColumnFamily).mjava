        public ByNameColumnIterator(Iterator<CellName> iter, DecoratedKey key, ColumnFamily cf)
        {
            this.iter = iter;
            this.cf = cf;
            this.key = key;
        }

