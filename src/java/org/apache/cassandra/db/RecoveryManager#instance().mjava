    public synchronized static RecoveryManager instance() throws IOException
    {
        if (instance_ == null)
        {
            instance_ = new RecoveryManager();
        }
        return instance_;
    }

