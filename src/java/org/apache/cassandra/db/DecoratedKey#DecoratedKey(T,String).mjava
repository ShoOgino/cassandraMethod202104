    @Deprecated
    public DecoratedKey(T token, String key)
    {
        super();
        assert token != null;
        this.token = token;
        this.key = key == null ? null : key.getBytes(FBUtilities.UTF8);
    }

