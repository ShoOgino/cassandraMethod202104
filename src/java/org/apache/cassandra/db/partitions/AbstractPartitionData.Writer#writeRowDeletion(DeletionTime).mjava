        public void writeRowDeletion(DeletionTime deletion)
        {
            ensureCapacity(row);
            if (!deletion.isLive())
                deletions.set(row, deletion);

            collectStats(deletion);
        }

