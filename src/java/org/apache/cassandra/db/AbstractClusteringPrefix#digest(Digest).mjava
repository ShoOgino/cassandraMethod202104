    public void digest(Digest digest)
    {
        for (int i = 0; i < size(); i++)
        {
            ByteBuffer bb = get(i);
            if (bb != null)
                digest.update(bb);
        }
        digest.updateWithByte(kind().ordinal());
    }

