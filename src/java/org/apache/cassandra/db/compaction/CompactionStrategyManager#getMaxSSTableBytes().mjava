    public long getMaxSSTableBytes()
    {
        readLock.lock();
        try
        {
            return unrepaired.first().getMaxSSTableBytes();
        }
        finally
        {
            readLock.unlock();
        }
    }

