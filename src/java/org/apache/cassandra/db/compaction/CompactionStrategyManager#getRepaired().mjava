    @VisibleForTesting
    List<AbstractCompactionStrategy> getRepaired()
    {
        readLock.lock();
        try
        {
            return Lists.newArrayList(repaired.allStrategies());
        }
        finally
        {
            readLock.unlock();
        }
    }

