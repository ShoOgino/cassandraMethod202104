    @VisibleForTesting
    List<AbstractCompactionStrategy> getRepaired()
    {
        readLock.lock();
        try
        {
            return Lists.newArrayList(repaired);
        }
        finally
        {
            readLock.unlock();
        }
    }

