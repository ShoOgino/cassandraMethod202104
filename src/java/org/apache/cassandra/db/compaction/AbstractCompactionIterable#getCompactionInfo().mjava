    public CompactionInfo getCompactionInfo()
    {
        return new CompactionInfo(this.hashCode(),
                                  type,
                                  bytesRead,
                                  totalBytes);
    }

