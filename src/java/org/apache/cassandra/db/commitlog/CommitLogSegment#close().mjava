    /**
     * Close the segment file.
     */
    public void close()
    {
        if (closed)
            return;

        try
        {
            logFileAccessor.close();
            closed = true;
        }
        catch (IOException e)
        {
            throw new IOError(e);
        }
    }

