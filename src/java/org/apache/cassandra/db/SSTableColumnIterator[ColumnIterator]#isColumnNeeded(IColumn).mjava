    private boolean isColumnNeeded(IColumn column)
    {
        if (isAscending)
            return (column.name().compareTo(startColumn) >= 0);
        else
            return (column.name().compareTo(startColumn) <= 0);
    }

