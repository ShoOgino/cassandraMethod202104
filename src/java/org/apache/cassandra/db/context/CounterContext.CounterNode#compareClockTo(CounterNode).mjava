        public int compareClockTo(CounterNode o)
        {
            if (clock == o.clock)
            {
                return 0;
            }
            else if (clock > o.clock)
            {
                return 1;
            }
            // clock < o.clock
            return -1;
        }

