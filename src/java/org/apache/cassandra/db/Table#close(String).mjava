    // prepares a table to be replaced
    public static void close(String table) throws IOException
    {
        // yes, I see the irony.
        Table t = open(table);
        if (t != null)
            open(table).release();
    }

