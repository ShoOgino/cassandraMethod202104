    public ColumnFamily deserialize(DataInput in, int version) throws IOException
    {
        return deserialize(in, ColumnSerializer.Flag.LOCAL, TreeMapBackedSortedColumns.factory(), version);
    }

