                public boolean hasNext()
                {
                    if (isDone)
                        return false;

                    if (next == null)
                    {
                        next = atomReader.get();
                        if (next == null)
                        {
                            isDone = true;
                            return false;
                        }
                    }
                    return true;
                }

