    public void readRecords()
    {
        assert records.isEmpty();
        FileUtils.readLines(file).stream()
                 .map(LogRecord::make)
                 .forEach(records::add);
    }

