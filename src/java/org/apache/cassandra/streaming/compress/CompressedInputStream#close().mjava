    @Override
    public void close() throws IOException
    {
        synchronized(this)
        {
            if (readerThread != null)
            {
                readerThread.interrupt();
                readerThread = null;
            }
        }
    }

