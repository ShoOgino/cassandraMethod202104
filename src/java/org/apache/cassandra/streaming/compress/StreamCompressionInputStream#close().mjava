    /**
     * {@inheritDoc}
     *
     * Close resources except {@link #dataInputPlus} as that needs to remain open for other streaming activity.
     */
    @Override
    public void close()
    {
        currentBuf.release();
    }

