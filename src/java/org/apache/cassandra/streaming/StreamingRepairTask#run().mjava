    public void run()
    {
        if (src.equals(FBUtilities.getLocalAddress()))
        {
            initiateStreaming();
        }
        else
        {
            forwardToSource();
        }
    }

