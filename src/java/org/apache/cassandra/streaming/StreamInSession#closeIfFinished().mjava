    public void closeIfFinished()
    {
        if (files.isEmpty())
        {
            if (callback != null)
                callback.run();
            sessions.remove(context);
        }
    }

