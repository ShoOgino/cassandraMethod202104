        public ResultSet build() throws InvalidRequestException
        {
            if (current != null)
            {
                addInputRow(this);
                resultSet.addRow(getOutputRow());
                reset();
                current = null;
            }
            return resultSet;
        }

