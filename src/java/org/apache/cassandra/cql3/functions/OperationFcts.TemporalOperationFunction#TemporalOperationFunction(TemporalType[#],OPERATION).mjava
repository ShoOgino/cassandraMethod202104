        public TemporalOperationFunction(TemporalType<?> type,
                                         OPERATION operation)
        {
            super(type, type, operation, DurationType.instance);
        }

