	/**
	 * Stops monitoring.	This does not free buffered data.
	 * @see #close()
	 */
	public void stopMonitoring() {
		if (isMonitoring)
		{
            if (timer != null)
            {
                timer.cancel();
                timer = null;
            }
			isMonitoring = false;
		}
	}

