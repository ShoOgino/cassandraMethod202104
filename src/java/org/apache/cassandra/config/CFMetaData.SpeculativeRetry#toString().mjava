        @Override
        public String toString()
        {
            switch (type)
            {
            case PERCENTILE:
                return value + "PERCENTILE";
            case CUSTOM:
                return value + "MS";
            default:
                return type.toString();
            }
        }

