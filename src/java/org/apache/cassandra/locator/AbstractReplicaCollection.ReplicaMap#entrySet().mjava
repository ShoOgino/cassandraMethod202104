        @Override
        public Set<Entry<K, Replica>> entrySet()
        {
            Set<Entry<K, Replica>> es = entrySet;
            if (es == null)
                entrySet = es = new EntrySet();
            return es;
        }

