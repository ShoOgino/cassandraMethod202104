    public static int countFull(ReplicaCollection<?> liveReplicas)
    {
        int count = 0;
        for (Replica replica : liveReplicas)
            if (replica.isFull())
                ++count;
        return count;
    }

