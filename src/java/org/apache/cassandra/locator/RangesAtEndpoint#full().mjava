    public RangesAtEndpoint full()
    {
        RangesAtEndpoint coll = fullRanges;
        if (fullRanges == null)
            fullRanges = coll = filter(Replica::isFull);
        return coll;
    }

