    public String toString(boolean withPort)
    {
        if (withPort)
        {
            return toString(address, port);
        }
        else
        {
            return address.toString();
        }
    }

