    public int getReplicationfactor(String table)
    {
        int total = 0;
        for (int repFactor : datacenters.get(table).values())
            total += repFactor;
        return total;
    }

