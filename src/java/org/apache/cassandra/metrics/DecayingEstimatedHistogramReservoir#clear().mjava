    @VisibleForTesting
    public void clear()
    {
        final int bucketCount = decayingBuckets.length;
        for (int i = 0; i < bucketCount; i++)
        {
            decayingBuckets[i].reset();
            buckets[i].reset();
        }
    }

