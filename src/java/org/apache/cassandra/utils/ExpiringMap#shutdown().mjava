    public void shutdown()
    {
        while (!cache.isEmpty())
        {
            try
            {
                Thread.sleep(100);
            }
            catch (InterruptedException e)
            {
                throw new AssertionError(e);
            }
        }
        timer.cancel();
    }

