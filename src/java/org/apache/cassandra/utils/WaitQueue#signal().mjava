    /**
     * Signal one waiting thread
     */
    public void signal()
    {
        if (queue.isEmpty())
            return;
        Iterator<Signal> iter = queue.iterator();
        while (iter.hasNext())
        {
            Signal next = iter.next();
            if (next.signal())
            {
                iter.remove();
                return;
            }
        }
    }

