        /**
         * Cleans up the Builder instance before recycling it.
         */
        private void cleanup()
        {
            quickResolver = null;
            Arrays.fill(values, null);
            count = 0;
            detected = true;
            auto = true;
        }

