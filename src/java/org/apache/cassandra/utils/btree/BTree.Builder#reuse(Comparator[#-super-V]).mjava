        private void reuse(Comparator<? super V> comparator)
        {
            this.comparator = comparator;
            quickResolver = null;
            count = 0;
            detected = true;
            auto = true;
        }

