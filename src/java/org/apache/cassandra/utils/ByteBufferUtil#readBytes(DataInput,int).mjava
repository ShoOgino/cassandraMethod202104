    public static byte[] readBytes(DataInput in, int length) throws IOException
    {
        assert length > 0;
        byte[] bytes = new byte[length];
        in.readFully(bytes);
        return bytes;
    }

