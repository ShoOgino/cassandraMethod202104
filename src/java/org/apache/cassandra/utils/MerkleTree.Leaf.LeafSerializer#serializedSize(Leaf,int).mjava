            public long serializedSize(Leaf leaf, int version)
            {
                long size = 1;
                if (leaf.hash != null)
                    size += leaf.hash().length;
                return size;
            }

