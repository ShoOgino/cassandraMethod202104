        protected final E consume()
        {
            candidate = queue.poll();
            if (candidate == null)
                return endOfData();
            return candidate.item;
        }

