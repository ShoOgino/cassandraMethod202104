        public ByteArrayWrapper(byte[] data)
        {
            if ( null == data )
            {
                throw new NullPointerException();
            }
            this.data = data;
        }

