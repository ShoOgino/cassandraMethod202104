            public Reclaimer reclaimImmediately(Cell cell)
            {
                onHeap().release(cell.name().dataSize() + cell.value().remaining());
                return this;
            }

