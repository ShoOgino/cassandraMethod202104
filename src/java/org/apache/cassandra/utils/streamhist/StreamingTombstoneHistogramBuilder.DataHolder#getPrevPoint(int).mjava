        private int getPrevPoint(int index)
        {
            if (index > 0)
                if (data[index - 1] != EMPTY)
                    return (int) (data[index - 1] >> 32);
                else
                    return -1;
            else
                return -1;
        }

