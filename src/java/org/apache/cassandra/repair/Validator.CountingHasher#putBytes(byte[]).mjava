        public Hasher putBytes(byte[] bytes)
        {
            count += bytes.length;
            for (Hasher h : underlying)
                h.putBytes(bytes);
            return this;
        }

