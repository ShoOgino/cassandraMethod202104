        public final RangeIterator<K, D> build()
        {
            switch (rangeCount())
            {
                case 0:
                    return null;

                case 1:
                    return ranges.poll();

                default:
                    return buildIterator();
            }
        }

