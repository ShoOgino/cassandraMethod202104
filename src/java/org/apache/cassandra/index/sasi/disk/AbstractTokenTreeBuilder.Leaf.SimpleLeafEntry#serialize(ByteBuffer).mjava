            @Override
            public void serialize(ByteBuffer buf)
            {
                buf.putShort((short) type().ordinal())
                   .putLong(token)
                   .putLong(partitionOffset)
                   .putLong(rowOffset);
            }

