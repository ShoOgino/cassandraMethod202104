        public UnfilteredRowIterator next()
        {
            if (nextPartition == null)
                prepareNext();

            UnfilteredRowIterator toReturn = nextPartition;
            nextPartition = null;
            return toReturn;
        }

