        public Object readValue(ChannelBuffer cb)
        {
            switch (this)
            {
                case COMPRESSION:
                    return CBUtil.readString(cb);
                default:
                    throw new AssertionError();
            }
        }

