    public int serializedValueSize(Object value)
    {
        switch (this)
        {
            case CUSTOM:
                return 2 + ((String)value).getBytes(Charsets.UTF_8).length;
            default:
                return 0;
        }
    }

