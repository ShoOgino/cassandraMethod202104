    boolean isSafeToDelete(byte[] bytes) throws IOException
    {        
        for ( byte b : bytes )
        {
            if ( b == 1 )
                return false;
        }
        return true;
    }

