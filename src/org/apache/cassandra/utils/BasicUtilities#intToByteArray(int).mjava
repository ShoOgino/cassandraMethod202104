	public static byte[] intToByteArray(int arg)
	{      
        byte[] retVal = new byte[4];
        ByteBuffer bb= ByteBuffer.wrap(retVal);
        bb.putInt(arg);
        return retVal; 
	 }

