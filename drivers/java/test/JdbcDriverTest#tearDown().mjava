    @Override
    protected void tearDown()
    {
        try
        {
            if (con != null)
            {
                deleteData();
                con.close();
                con = null;
            }
        } 
        catch (SQLException e)
        {
            fail(e.getMessage());
        }
    }

