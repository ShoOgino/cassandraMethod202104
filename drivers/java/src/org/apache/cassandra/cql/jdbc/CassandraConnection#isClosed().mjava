    /**
     * @return
     * @throws SQLException
     */
    public boolean isClosed() throws SQLException
    {
        if (cassandraCon == null)
            return true;

        return !cassandraCon.isOpen();
    }

