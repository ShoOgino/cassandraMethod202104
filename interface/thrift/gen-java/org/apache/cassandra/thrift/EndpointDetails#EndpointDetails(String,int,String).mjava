  public EndpointDetails(
    String host,
    int port,
    String datacenter)
  {
    this();
    this.host = host;
    this.port = port;
    setPortIsSet(true);
    this.datacenter = datacenter;
  }

