    public execute_prepared_cql_query_args(
      int itemId,
      List<ByteBuffer> values)
    {
      this();
      this.itemId = itemId;
      setItemIdIsSet(true);
      this.values = values;
    }

