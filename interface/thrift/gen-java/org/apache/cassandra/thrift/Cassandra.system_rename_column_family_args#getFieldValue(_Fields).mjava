    public Object getFieldValue(_Fields field) {
      switch (field) {
      case KEYSPACE:
        return getKeyspace();

      case OLD_NAME:
        return getOld_name();

      case NEW_NAME:
        return getNew_name();

      }
      throw new IllegalStateException();
    }

