  public IndexClause(
    List<IndexExpression> expressions,
    byte[] start_key,
    int count)
  {
    this();
    this.expressions = expressions;
    this.start_key = start_key;
    this.count = count;
    setCountIsSet(true);
  }

