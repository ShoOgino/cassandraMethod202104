    public execute_prepared_cql_query_args(
      int itemId,
      List<String> values)
    {
      this();
      this.itemId = itemId;
      setItemIdIsSet(true);
      this.values = values;
    }

