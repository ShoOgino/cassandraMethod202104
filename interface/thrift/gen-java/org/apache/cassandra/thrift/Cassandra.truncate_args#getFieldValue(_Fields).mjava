    public Object getFieldValue(_Fields field) {
      switch (field) {
      case KEYSPACE:
        return getKeyspace();

      case CFNAME:
        return getCfname();

      }
      throw new IllegalStateException();
    }

