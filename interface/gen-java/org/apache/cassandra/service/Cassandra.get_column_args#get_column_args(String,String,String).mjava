    public get_column_args(
      String tablename,
      String key,
      String columnPath)
    {
      this();
      this.tablename = tablename;
      this.key = key;
      this.columnPath = columnPath;
    }

