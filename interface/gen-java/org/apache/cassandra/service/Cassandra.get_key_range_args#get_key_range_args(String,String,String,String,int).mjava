    public get_key_range_args(
      String keyspace,
      String column_family,
      String start,
      String finish,
      int count)
    {
      this();
      this.keyspace = keyspace;
      this.column_family = column_family;
      this.start = start;
      this.finish = finish;
      this.count = count;
      setCountIsSet(true);
    }

