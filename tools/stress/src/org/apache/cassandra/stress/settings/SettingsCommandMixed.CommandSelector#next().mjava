        public Command next()
        {
            while (remaining == 0)
            {
                remaining = count.next();
                cur = selector.sample();
            }
            remaining--;
            return cur;
        }

