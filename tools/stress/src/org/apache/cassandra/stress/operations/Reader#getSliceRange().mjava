    private SliceRange getSliceRange()
    {
        return new SliceRange()
                    .setStart(new byte[] {})
                    .setFinish(new byte[] {})
                    .setReversed(false)
                    .setCount(session.getColumnsPerKey());
    }

