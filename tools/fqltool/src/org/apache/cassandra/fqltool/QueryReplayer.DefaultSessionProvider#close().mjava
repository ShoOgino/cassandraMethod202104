        public void close()
        {
            for (Session s : sessionCache.values())
            {
                try
                {
                    s.close();
                    s.getCluster().close();
                }
                catch (Throwable t)
                {
                    logger.error("Could not close connection", t);
                }
            }
        }

