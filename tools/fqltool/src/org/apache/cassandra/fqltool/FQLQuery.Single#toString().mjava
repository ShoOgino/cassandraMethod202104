        @Override
        public String toString()
        {
            return String.format("%s: Query: [%s], valuecount : %d",
                                 super.toString(),
                                 query,
                                 values.size());
        }

